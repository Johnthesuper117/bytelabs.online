# Install required packages
  tasks:
    - name: Install dependencies
      apt:
        name:
          - git
          - python3
          - python3-pip
        state: present
        update_cache: yes

# run soundboard setup script
    - name: Run Soundboard setup script
      command: bash soundboardSetup.sh
      args:
        chdir: /workspaces/bytelabs.online
